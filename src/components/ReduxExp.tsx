import React from 'react';
/** Redux : A conatiner where you can store your whole application data.
 *          so we call it to stote management
 *          it dosen't belong to the component state
 * use: it provide data all over the application
 */

const ReduxComponent: React.FC = () => {
    return (
        <div className="redux-container">
            <h1>
                This is Redux exmaple
            </h1>
        </div>
    );
}

export default ReduxComponent;
